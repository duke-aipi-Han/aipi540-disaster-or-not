# Data

Place Kaggle's Natural Language Processing with Disaster Tweets files here:

```text
train.csv
test.csv
sample_submission.csv
```

The training scripts use `train.csv`. Raw Kaggle archives can stay in this folder locally, but should not be committed if they are too large.
