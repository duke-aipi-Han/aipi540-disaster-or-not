# Model Artifacts

Training scripts write model artifacts here.

Baseline:

```text
tfidf_logreg.joblib
baseline_metrics.json
```

Transformer:

```text
transformers/<model-name>/
transformer_metrics_<model-name>.json
```
